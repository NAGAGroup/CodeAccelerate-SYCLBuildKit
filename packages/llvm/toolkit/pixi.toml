# naga-sycl-toolkit package (full toolkit)
# This is a sub-package that only defines [package], not [workspace]

[package]
name = "naga-sycl-toolkit"
version = "2025.12.1"

[package.build]
backend = { name = "pixi-build-rattler-build", version = "*" }

[package.run-dependencies]
naga-sycl-toolkit-devel = { path = "../devel" }
