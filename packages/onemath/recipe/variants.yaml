# Variant configuration for oneMath build
# Used by pixi-build-rattler-build backend
#
# The naga-adaptivecpp-toolkit IS the compiler toolchain.
# No compiler variants needed here - only stdlib.

c_stdlib:
  - "sysroot"

c_stdlib_version:
  - ">=2.34"